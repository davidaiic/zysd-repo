<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="model"
            type="com.manle.phone.android.yaodian.bean.GoodsDetailBean.ServerBean" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/dp_15">

        <ImageView
            android:id="@+id/iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            imageUrl="@{model.icon}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_se_csjz" />

        <ImageView
            android:id="@+id/nextIv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_right"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/titleTv"
            android:layout_width="@dimen/dp_0"
            android:layout_height="wrap_content"
            android:paddingHorizontal="@dimen/dp_15"
            android:textColor="@color/color_333333"
            android:textSize="@dimen/dp_14"
            android:text="@{model.serverName}"
            app:layout_constraintBottom_toTopOf="@+id/subTv"
            app:layout_constraintLeft_toRightOf="@+id/iv"
            app:layout_constraintRight_toLeftOf="@+id/nextIv"
            app:layout_constraintTop_toTopOf="@+id/iv"
            tools:text="我要比价" />

        <TextView
            android:id="@+id/subTv"
            android:layout_width="@dimen/dp_0"
            android:layout_height="wrap_content"
            android:paddingHorizontal="@dimen/dp_15"
            android:textColor="@color/color_999999"
            android:textSize="@dimen/dp_12"
            android:text="@{model.desc}"
            app:layout_constraintBottom_toBottomOf="@+id/iv"
            app:layout_constraintLeft_toRightOf="@+id/iv"
            app:layout_constraintRight_toLeftOf="@+id/nextIv"
            app:layout_constraintTop_toBottomOf="@+id/titleTv"
            tools:text="患者招募，助力临床" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>